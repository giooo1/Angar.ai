<!--
PASTE YOUR WEEK-1 SYSTEM PROMPT BELOW THIS LINE.

This file is the v0 baseline prompt for the eval harness. The harness will
refuse to run until this file contains a real prompt — see eval/prompt.py
for the sentinel check.

Once pasted:
- Treat this file as immutable. Future iterations go to v1.md, v2.md, etc.
- The harness runs `--prompt-version v0` by default.
- Delete this entire HTML comment block when you paste so the harness loads
  only the prompt body.

Why a separate file (not inlined in code): so `git diff` on a prompt change
is easy to review, and so the eval results JSON can record `prompt_version`
as a meaningful tag rather than a code revision hash.
-->
